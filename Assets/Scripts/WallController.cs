using UnityEngine;
using System.Collections;

public class WallController : MonoBehaviour {

//	private Transform m_wallTransform;
//	private float defaultDistance = 5;//扇形距离
//	private float defaultAngle = 60;//扇形的角度

	void Start () {
//		m_wallTransform = transform;
	}
	
	void Update () {
//		Vector3 personPosition = FirstPerson.m_chaTrans.position;
//		personPosition.y = m_wallTransform.position.y;
//		Vector3 norVec = personPosition + FirstPerson.m_chaTrans.rotation * Vector3.forward * 5;
//		float distance = Vector3.Distance(personPosition, m_wallTransform.position);//距离
//		Debug.DrawLine(personPosition, norVec, Color.red);
//
//		if (distance < defaultDistance) {
//			Vector3 temVec = m_wallTransform.position;
//			Debug.DrawLine(personPosition, temVec, Color.green);
//			float angle = Vector3.Angle (norVec - personPosition, temVec - personPosition);
//			if (angle < defaultAngle) {
//				
//			}
//		}

	}
}